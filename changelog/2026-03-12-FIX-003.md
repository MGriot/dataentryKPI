# Changelog: 2026-03-12-FIX-003

## Task ID & Title
FIX-003: Analysis Tab: Resolve `NameError` in Dashboard Plotting

## Timestamp
[12:00:00]

## The "Why"
A typo in a list comprehension was causing a `NameError` whenever the Global Dashboard tried to map timeline labels to chart indices. This prevented the multi-year timeline fix from functioning correctly.

## Verification
- **Code Fix**: Replaced undefined variable `l` with `label` in `load_dashboard_data` for both Target 1 and Target 2 plotting logic.
- **Functionality**: The Global Dashboard now successfully renders continuous timelines across multiple years without crashing.
