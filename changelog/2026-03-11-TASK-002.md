# Changelog: 2026-03-11-TASK-002

## Task ID & Title
TASK-002: Database Evolution: Global KPI Splits Table

## Timestamp
[10:45:00]

## The "Why"
To support standardized annual distribution templates across multiple plants, a centralized storage location is required. The `global_kpi_splits` table in `db_kpi_templates.db` provides this standardized storage while maintaining logical separation from plant-specific data.

## Verification
- **Schema Update**: Added `global_kpi_splits` table to `src/data_access/setup.py`.
- **Execution**: Successfully ran `python -m src.data_access.setup`.
- **DB Check**: Verified that the table is created with columns: `id`, `name`, `year`, `repartition_logic`, `repartition_values`, `distribution_profile`, and `profile_params`.
