# Changelog: 2026-03-11-TASK-003

## Task ID & Title
TASK-003: Database Evolution: Add Global Split Reference to Annual Targets

## Timestamp
[11:00:00]

## The "Why"
To allow individual KPI targets per plant to reference the newly created standardized distribution templates, a foreign key reference (`global_split_id`) was added to the `annual_targets` table. This enables the repartition logic to fetch global templates instead of relying solely on plant-specific configurations.

## Verification
- **Schema Update**: Added `global_split_id` column to `annual_targets` in `src/data_access/setup.py`.
- **Execution**: Successfully ran `python -m src.data_access.setup`.
- **DB Check**: Verified that the column `global_split_id` (INTEGER) exists in the `annual_targets` table within `db_kpi_targets.db`.
